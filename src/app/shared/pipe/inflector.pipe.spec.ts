import { InflectorPipe } from './inflector.pipe';

describe('InflectorPipe', () => {
  it('create an instance', () => {
    const pipe = new InflectorPipe();
    expect(pipe).toBeTruthy();
  });
});
